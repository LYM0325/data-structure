/*线性表
当线性表的元素个数为0称为空表

网课中一些缩写
ADT  抽象数据类型名
Data  数据元素之间逻辑关系的定义
Operation  操作
endADT

InitList(*L);//初始化操作
ListEmpty(*L);//判断线性表是否为空表，若为空表返回true，否则返回false
ClearList(*L);//将线性表清空
GetElem(L,i,*e);//将线性表中第i个位置的值返回给e
LocateElem(L,e);//在线性表中查找与给定值e相等的元素，如果查找成功返回该元素在表中的序号，否则返回0表示失败
ListInsert(*L,i,e)//在线性表中第i个位置插入新元素e
ListDelect(*L,i,*e)//删除线性表中第i个位置的元素，并用e返回其值
ListLength(L)//返回线性表的元素的个数 
*/
